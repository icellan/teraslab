## E2E-green campaign — subagent fix wave (2026-08-14)
- Task 47 (election determinism, 4500d57): code+review DONE (APPROVE_WITH_NITS). Important finding folded into task #50 (steady-state deviation needs active repair to converge placement; liveness-only). Minors deferred to final whole-branch pass: (M2) rank_master_candidate at coordinator.rs:10843 called with candidate id as _prev_master — compute tier from is_subset/was_evicted directly; (M3) Task #22 comment at :10767 should state the surviving timeout-absent-candidate availability gap; (M4) reheal_exchange_term slot theoretical leak on exchange-thread panic — accepted.
- Task 49 (FreeRegion delete-wins, 4d868ca on fix/09-freeregion-replay): DONE_WITH_CONCERNS, review in flight. Follow-up task #55 (tombstone intent journaling).
- Task 48 (08 harness accounting, 1474f14 on fix/08-harness-accounting): DONE, review in flight.
- Task 51 (replica-ACK durability): fixer in flight (fix/17-replica-ack-durability).
- Task 54 (migration blob baseline): fixer in flight (fix/11-migration-blob-baseline).
- Merge order once green: 4500d57 (already on branch) → fix/09 → fix/17 → fix/08 → fix/11blob; full suite + clippy + fmt on merged result; #51 gets bitcoin-expert+security-auditor round pre-push; then CI validation run.
- Review round 1 results: 4500d57 APPROVE_WITH_NITS (accepted); 4d868ca APPROVE_WITH_NITS but 2 Important → fix round dispatched (fuzzy-snapshot alias window: refresh offset_owners on skip-already-indexed create replays + evict-all-matching consideration; delete_inner doc still teaches index-wins); 1474f14 REQUEST_CHANGES → fix round dispatched (restore batch 5 — 1-item batches can't be partials and client retry chain 31.4s makes them always blow the 8s budget; degraded partials must stay in retry set not credited; phantom-credit guard for out-of-range item_index; extractor scenario-failed suppression; revert-catching seam tests).
- Task 54 (ExternalRef wire section, 0b08433+46a116c): review APPROVE_WITH_NITS, minors applied directly (distinct field values 46a116c), MERGED into fix/e2e-scenarios-green (ff to 46a116c). Real defect was missing extended wire section (offset 70+), not blob bytes; fix covers baseline+repair+delta+catch-up via one shared helper.
- Task 51 (replica-ACK never-skip, 6169fe0 on fix/17-replica-ack-durability): fixer DONE. Mechanism = sender position-burn + Gap-NAK relabel puts fresh content at watermark-covered positions → skipped-but-ACKed. bitcoin-expert + security-auditor round IN FLIGHT (durability/consensus-critical gate). New task #56 for redo-flush fsync residual (power-loss window).
- 6169fe0 security-auditor: APPROVE_WITH_NITS. Verified-safe: ACK max() not poisonable (sender Ok{through>last} never credits quorum), barrier→tracker→ACK fail-closed, stale-key gate still fences gen-less ops (not vouchable). RISK REGISTER: same-epoch replay of gen-less ops on tracked stream now applies with effect (on-path replayer, HMAC-valid capture, 5-min skew window, no key needed; worst case self-healing gaps) + bounded fsync amplification on replay. Closure = per-direction MAC'd sequence (replay-nonce design, docs branch only — NOT in tree); interim hardening lever = narrow max_clock_skew (auth.rs:115). Awaiting bitcoin-expert verdict on same commit.
- 08 fix round 2981275 DONE (F1-F6 closed, F8 skipped w/ justification, mutation-verified seam tests); re-review in flight.
- Task 49 (FreeRegion): fix round 9b6a669 (SlotOwners multimap, prescription-rejection verified correct by re-reviewer) re-review APPROVE, MERGED (400ada4). Task COMPLETED.
- Task 48 (08 harness): re-review APPROVE (all closed, F2 latent-not-live: degraded unreachable at RF>1 by config validation), MERGED (d6648d0). Task COMPLETED.
- 6169fe0 bitcoin-expert: APPROVE_WITH_NITS. F1 zombie-Delete tombstone veto → task #57. F2 chunk-staging splice + F3 metric + F4 generation-guard tests + F5 doc drift → follow-up round dispatched to original fixer. Consensus lenses clean: Create embedded-generation guard is load-bearing and verified; ACK values byte-identical all shapes.
- PENDING: fix/17 follow-up commit → verify → merge → full merged gate (test --all, clippy, fmt, client crate) → push → CI validation run (default flags) → analyze 06/08/09/11/15/17.
- fix/17 follow-up b13b221 (splice conflict close + covered-reapply metric + generation-guard pin + zombie-delete residual pin + doc drift) verified by orchestrator inspection, MERGED (5ad6ea7). Merge train complete: e2e-green = b18f13c + 4500d57 + blob(0b08433,46a116c) + 08(2981275) + 09(4d868ca,9b6a669) + 17(6169fe0,b13b221). Full merged gate running in background; on green → push → CI default-flags validation run.
- REGRESSION DIAGNOSIS correction (run 31787458246): "creates collapse" cluster was a MISDIAGNOSIS — all quoted lines were passing should-panic unit tests interleaved in raw logs (ALWAYS read SUMMARY.md, never raw grep). Degraded acks unreachable at RF=2/reject (08 reviewer was right). REAL failures: 08 = master-view divergence post-heal (masters_per_node=[N,N,Y], epochs agree, holders all Y) at 8a.3, 8c/8d never ran; 09 = pre-existing red MUTATED: three DIFFERENT tables all at ver=8 (masters sum 6424/4096, node2 kept 2-node-era table) — table-content-vs-version integrity bug, unit-testable fingerprint from status JSONs; 10 = genuine regression: 1×6.2s delete stall on 13-sample p99 gate + 32% throughput drop + footer-unreadable 2.9x amplification (51k→149k, engine enumeration/snapshot paths) — suspect recovery/FreeRegion area, needs bisect. Harness fix candidates (small): p99 min-sample floor; [selftest] prefix on should-panic fixture text; reconcile per-item None handling (latent).
## Wave 2 review round 1 (2026-08-15)
- w2harness c17257e REQUEST_CHANGES (gate clause was provably no-op at scenario_06 — read commit-time fields; real window is commit→activation) → rework f907ed6 (activation-time clauses: target>0 + serving==target; dissolves the 07-hang finding too) → re-review in flight.
- w2election d6ec64c REQUEST_CHANGES (F2 strict count<max classifier consumes live-moving counts → manufactures equal-version divergence under load with zero faults; F1/F7 verified sound) → rework dispatched (material-lag rule: count==0 || 2*count<max, monotone-threshold argument restored; + 4 minors).
- w2completion 4bba562 APPROVE_WITH_NITS (data-safety verified: single-master rollback, epoch-exact orphan guard, code=22 exemption sound) → nit round dispatched (unreadable-but-indexed burns escalation attempts; producer↔parser contract test; single-lock retire; (code=19: tighten).
- w2staging 9d609bf review in flight (bonus fix confirmed red-first: migration paths mis-decoded redo-full Busy as code=3 shard failure). Task #61 filed (staging TTL residual).
- w2blobflag: fixer resumed post-limit, still working (EXTERNAL flag recovery data loss — hard blocker for 11).
## Wave 2 complete (2026-08-15)
- ALL FIVE branches reviewed to APPROVE + merged: w2harness (c17257e+f907ed6+23e529a, activation-time gate), w2election (d6ec64c+a2ad7e2, honest views + material-lag classifier), w2completion (4bba562+199b91e, terminal escalation + per-shard orphan gate), w2blobflag (3249554+4e782b1, quarantine + scrape counters), w2staging (9d609bf+c0ddd1b+67a18ee, hoisted pressure gate + busy retry). One cross-branch seam (stream_shard_baseline 8th arg vs new escalation call site) fixed by orchestrator (886b04b). Full gate green (known cluster_partition parallel-load flake verified green standalone). Pushed 5ad6ea7..886b04b; CI validation run 31875324685.
- Follow-up tasks filed this wave: #59-#65.
## Run 31875324685 @ 886b04b: 7/14 (2026-08-15)
- RECOVERED from 5/14: 07 + 10 green again (wave-2 fixes validated). Red: 05/06/08/09/11/15/17.
- 05: master_failed 14→0; residual = 1 under-replicated record → PASSIVE REPAIR GAP (task #50). 15.3: 77/295 one-holder records, unrepaired through 22 census rounds/15s → SAME GAP at scale. #50 builder DISPATCHED (feat/event-driven-repair).
- 06: raw convergence throughput at 120s bound (node3 saturated, 1246+1252 active migs, handoffs moving; new activation gate correctly holds). Candidate fix: task #60 (re-heal re-push churn).
- 11: target divergence STILL: 4101/4096 (5 shards) @ ver=4 serving==target everywhere; 17: 4097 (1 shard) @ ver=10 → divergence diagnoser dispatched.
- 08: PROGRESS past old 8a.3; new fail = client partition map still routes to isolated node3 after 75s (masters={1,2,3} v2). 09: NEW signature 0/3 nodes ready, versions:[] — boot/ready diagnoser dispatched (watch for recovery-wave regressions).
- 15's polled census + [selftest] prefixes + SUMMARY extractor all working as designed.
## Run-3 diagnoses complete (2026-08-15)
- 09 = SEGMENT ENGINE composition seam: SegmentAllocator::free journals NOTHING → the whole 4d868ca/9b6a669 delete-wins eviction is DEAD CODE on clustered E2E nodes (segment engine); phantom at max offset → frontier recovery FATAL exit(1). Fixer: fix/w3-segment-delete-durability (journal FreeRegion + tolerant frontier + harness ready-message).
- 08 = pre-existing CLIENT defect: refresh_partition_map adopts first-answering pool's map (stale minority v2 from host-reachable isolated node3), is_alive discarded. Fixer: fix/w3-client-map-freshness (highest-version wins + liveness).
- 11/17 divergence = counter-perfect diagnosis: TRIGGER unshippable keys → terminal rollback; SUSTAINER = election candidate set is det-pair ONLY, holder outside pair is UNEXPRESSIBLE (5 shards sc11 = exactly 4101-4096; 1 sc17). F7 fires but is det-baseline-blind to third-holder divergence; F1/F2 exonerated (no view asymmetry, no flap). Fixer: fix/w3-election-candidates (A: view-evidenced candidates, B: reduced manifests on skipped keys, C: master-only rollback guard).
- 05/15 = passive repair gap → #50 builder (feat/event-driven-repair) in flight.
- 06 residual = convergence throughput (churn task #60 is the candidate; deferred until this wave lands).
- Wave-3 review round: c097ebb (event repair) APPROVE_WITH_NITS but both P2s pre-arm-gating (untracked resync dedup; refutation-shaped MembershipChanged arming) → fix round dispatched. 70790b2 (client map) REQUEST_CHANGES — 2 P0s (alive_ids not reconciled with assignments → suspect-window outage; restarted-node everyone-dead map prunes to one pool), P1 single-flight + P1 sequential-dial cost, ABBA inversion → full fix round dispatched. Election-candidates fixer + segment-delete fixer still building.
- Wave-3 landings: fix/w3-client-map-freshness (70790b2+97b67db) re-review APPROVE_WITH_NITS → MERGED (ff 97b67db); follow-up task #68. fix/w3-election-candidates (81550ef + f8b3c8e comment fix) review APPROVE_WITH_NITS → MERGED (d087c5d); adopt_external_master gated to same-term re-heal only, no-loss displaced-slot guard verified; finding-1 (replica-abort leaves under-RF until sweep) = the #50 arming closure; rf=1 permanent-deviation minor noted (outside validated config); production-path adoption test gap accepted (CI run is the proof). Pending: repair-feature re-review (ab62802), segment-delete fixer.
## Wave 3 complete (2026-08-15)
- ALL FIVE branches reviewed + merged: w3climap (70790b2+97b67db), w3electcand (81550ef+f8b3c8e), w50repair (c097ebb+ab62802+981a824), w3segdel (137e52d+bfdbb45), all APPROVE/APPROVE_WITH_NITS after fix rounds. Full gate green. Pushed 886b04b..de3ac56. CI: default run 31904708491 + arm_sweep qualification run 31904714169 (the #50 vehicle — 15/05 need the armed sweep). Worktrees cleaned (4). New follow-up tasks: #68 (client polish), #69 (zeroed-tombstone wedge).
- Segment used-floor substitution (lower bound, safe direction) + finding-2 unreachability proof documented at call site.
## Runs @ de3ac56 (2026-08-15): default 31904708491 = 9/14 HIGH-WATER MARK (05+08 newly green); armed 31904714169 = 2/14 CATASTROPHE
- Default red: 06 (rs:40 migration wait), 09 (rs:81 ready wait, 268s — further than before?), 11 (rs:817 — NEW sub-test line, past the old migration wait!), 15+17 (common/mod.rs:2456 RF census — expected without armed sweep).
- Armed red: nearly everything incl. 02 basic ops → the armed sweep/event-repair is toxic under load (suspect: resync storms). Qualification gate worked as designed — sweep NOT ready for default-on.
## Wave 4 dispatched (2026-08-15)
- Armed-sweep catastrophe ROOT-CAUSED (very high conf): derive compares live local counts vs FORMATION-FROZEN retained exchange view (single writer :3752, stable cluster never re-exchanges) → full-set permanently empty → everything classified under-replicated → 300-650-shard storms/20s → write fences → wedged nodes. Fixer: fix/w4-sweep-freshness-fence (self-entry freshness witness + honest staleness guards + volume cap). Secondary: leaked migration_phase gauges (file later), misleading replicas log field.
- Default-run: fix/w4-1110-big-record-contract (test defect, 11.10 sole non-contract site) + fix/w4-terminal-abort-replan (09 deadlock: frozen tasks hold active_count vs re-heal gate; "awaiting membership change" never fires) in flight. 06 = same silent-node residual (rotated node3→node2; no resource capture for 06 — consider adding).
## Wave 4 complete (2026-08-16)
- ALL merged + gate green + pushed de3ac56..f64435e: fix/w4-1110-big-record-contract (add1ca2, self-reviewed), fix/w4-sweep-freshness-fence (8f132e4, spot-checked — kills the armed catastrophe), fix/w4-terminal-abort-replan (5b0d8b5+938543a, REQUEST_CHANGES→APPROVE with generation-stamp guard against the retry-overlap acked-write-loss window). New follow-up #72 (stale retained view in election provider). CI: default 31911172622 + armed 31911177514 in flight — armed is the sweep-fence proof + #50 qualification retry.
## Wave 5 (2026-08-16)
- Armed diag: sweep exonerated for 15 — orphan cleanup writes AUTHORITATIVE ClientDelete tombstones (69/69/69 identity) permanently vetoing repair creates (RULE-DS); reclaim_held_copy exists for exactly this → fix/w5-orphan-reclaim-authority in flight (also the code=19 family root: 1886 aborts sc09, 135 sc11). Sweep needs drain backpressure (sc08 armed: 442 passes, 512-conn self-DoS of majority peer; sc04: 971 stacked streams) → fix/w5-sweep-drain-backpressure in flight.
- Default diag: parking WORKED (09 counters 0/0, 4096/4096); 05/08 = pre-existing variance lotteries; 09 new blocker = 1 IoError-classified replay failure vetoes boot (494 siblings tolerated) → fix/w5-replay-absence-tolerance in flight; 11 advanced to a 30s-wait-vs-30s-cooldown mismatch. STRUCTURAL CEILING named: exchange view_size=1 under load = divergence generator (task #73).
- fix/w5-harness-variance (15323d5: 11.11 90s budget + concurrent 8d.2 batches, P(zero) 57%→12%) self-reviewed + MERGED.
## Wave 5 complete (2026-08-16)
- ALL merged + gate green + pushed f64435e..8d73b10: w5harness (15323d5), w5tombauth (cb93e59 + 773f93a doc, bitcoin-expert APPROVE_WITH_NITS — F1 heal-source fallback → task #74), w5sweepbp (48aac4e, spot-checked), w5replaytol (9d4a90a, spot-checked). New tasks #73 (view_size=1 structural), #74 (heal-source quorum fallback). CI: default + armed runs dispatched. The tombstone-authority fix is the big swing: expected to move 15-armed AND shrink the code=19 family in 05/09/11/17.
## Wave 6 dispatched (2026-08-16)
- Runs @ 8d73b10: default 8/14 (SCENARIO 11 GREEN FIRST TIME; 05 green), armed 7/14. Tombstone fix CONFIRMED decisive: code=19 sc09 1567→0, sc15 246→1 (the 1 = the single census violation, same txid). Sweep backpressure VERIFIED: 512-conn DoS gone (74929→0), drain gate perfect (in_flight_skipped=0 all 88 passes), resync conns=8/bulk=48 correctly split. Armed-09 cleared 9.1+9.2, died 9.3 with a 24-shard tail (was 1365).
- NEW mechanisms found: (1) catch-up re-proposal reverts a just-committed quiesce (coordinator.rs:5042, 6ms timeline proof) — default-09 + likely 07's ver=22 thrash; (2) armed-15 = positional-Vec settle predicate harness bug (false-positive risk!) on top of the #75 node-wedge; (3) armed-06 = mark_inbound_lost false "source died" (coordinator.rs:3424) fencing ~2400 shards → 37% NO_QUORUM; (4) RULE-DS veto is silent (receiver.rs:1890/:2417) — logging = the #74 diagnosis unlock. Fixers: fix/w6-quiesce-and-inbound (1+3), fix/w6-settle-and-veto-log (2+4). Tasks #75 (node-wedge diagnostic needed), #76 (pinned ack cursor).
## Wave 6 complete + campaign checkpoint (2026-08-16)
- Runs @ 50b8e2c: default 31940403992 = 7/14, armed 31940408347 = 7/14. Scenario 11 GREEN IN BOTH (repeat — durable). 05/14 flapped opposite directions; 08 alternates its two known signatures (rs:222 = #74 family, rs:1135 = 8d ~12% lottery); 06/07/09/15/17 = the tracked structural items.
- ASSESSMENT: six waves fixed every surgically-diagnosable defect (~30 product + ~12 harness, all reviewed+merged). Remaining reds map 1:1 to open DESIGN items: #73 (view_size=1 divergence generator — needs a consensus-design round: divergence-prevention gate vs exchange hardening), #74 (heal-source quorum fallback — alert-and-hold vs refuse posture, repo precedent says USER decision), #75 (node-wedge — needs a thread-dump watchdog instrument built first), #60 (re-heal churn), plus 8d variance-bound acceptance and the census-without-sweep pair (15/17 need the armed sweep qualified AND the #74 residual closed). Terminal for the fix-one-by-one loop; next moves are design decisions.
## Wave 7 dispatched (2026-08-16) — the design items, conservative postures per repo precedent
- #73: fix/w7-reheal-view-quorum (same-term re-heal refuses to install below view quorum; new-term activations untouched; liveness via SWIM-death→new-term path).
- #74: fix/w7-heal-source-quorum (fallback REFUSES without quorum-current evidence; alert-and-hold; permanent-refusal = correct when serving would be the double-spend).
- #75: feat/w7-stall-watchdog (event-loop heartbeat + lock-fingerprint stall dump + degraded /status on table-lock timeout).
- 8d odds: 13bb578 direct (8 slots, ~1.4% zero-records odds).
- #74 SHIPPED after 3 expert rounds (98f4076+a28ec4e+6ed602f): quorum-current-only selection, durable kind-discriminated parks (inbound state format now [shard:2][from:8][flags:1] — deliberate break), park-and-retry driver in the reactivation metric with 30s→300s park-only backoff, foreign completions can't clear parks, empty sources rejected. Honest scope: evidence gate, NOT per-key currency (full closure = #77 manifest design). Expert nit ledgered: backoff resets only on shrink — a shard parking mid-backoff inherits the long interval (fail-closed; reset-on-any-set-change would be better). #75 watchdog merged earlier (f53f536).
## Wave 7 complete (2026-08-16)
- ALL SHIPPED + gate green + pushed 50b8e2c..51b9061: #73 view-quorum re-heal gate (aa080f3), #74 heal-source quorum + durable kind-discriminated parks (3 expert rounds → APPROVE), #75 stall watchdog (9ddaa30), 8d odds (13bb578). Both CI runs dispatched. Every formerly-"design decision" item now has its conservative implementation shipped per repo posture precedent; remaining open tasks are follow-ups (#38/#42/#52-#65/#68/#69/#72/#76/#77), none scenario-blocking on their own.
## Runs @ 51b9061 (2026-08-16): default 9/14 (ties high-water, STRONGER composition: 05/07/11/14 green), armed 8/14 (ARMED HIGH-WATER, 07+08 green armed first time)
- Remaining: 06 both (watchdog now live — fingerprint expected!), 09 both (post-quiesce-fix layer), 15/17 census both (sweep-repair timing vs the new fail-closed parks — possible bound decision), 05-armed, 08-default (variance family), 10-armed rs:885 (new). Diagnoser dispatched.
## Wave 8 (2026-08-16) — diagnosis @ 51b9061 runs + four fixers
- Diagnosis: 06 = watchdog FINGERPRINTED the node-wedge (inbound_prune, migration→shard_table lock inversion); 09 = failed-batch terminal aborts with no self-retry + the #73 gate's asymmetry (below-quorum INITIAL activations still create divergent tables the gated re-heal then refuses to repair); 15/17 census = tombstone vetoes surfacing as generic TxNotFound (diagnosability) + 1-holder convergence needing the retry; 10-armed rs:885 = p99 gate with thin samples.
- fix/w8-harness-gates (b7f42fd): p95 tier for thin p99 samples + under-replicated records in diagnose dumps. MERGED.
- fix/w8-inbound-prune-lock-order (d7ff0d7): canon shard_table→migration restored at inbound_prune + 2 siblings; 153-site audit; red deadlock test 30s-hang→0.05s. MERGED (c22400d).
- fix/w8-abort-retry-and-veto-error (3a2bf1b+01ecb9a): failed batches self-retry via FailedBatchRedrive (2s→30s cap, epoch-fenced) + failed_retry_hold surviving cleanup_completed; RULE-DS vetoes NAMED at completion verify; manifest reduction on vetoed keys flag-gated DEFAULT-FALSE pending #78. bitcoin-expert REQUEST_CHANGES→APPROVE (retry was inert: cleanup deleted Failed entries before redrive fired; reduction was a default-on deletion authority). MERGED (a6647fc).
## Wave 8 final branch: activation view-quorum (2026-08-16) — 4 expert rounds, mechanism REPLACED in review
- Fixer (agent died pre-commit; work completed+committed by orchestrator as 395f777, merged f9e8144): extended the #73 hold to first-of-term activations.
- EMPIRICAL WEDGE before round-1 report even landed: segment_node_rejoins_and_takes_shard_ownership deterministic red — 2-node formation held forever (peer rejects exchange report until commit APPLIED (ERR_STALE_EPOCH); prompt-arm predicate committed_term>active_version false at bootstrap v1 = term 1; G8 refuses the 2→1 shrink so the term never advances).
- bitcoin-expert round 1 REQUEST_CHANGES: hold = TOTAL serving outage unbounded (P0-1); liveness invariant false — voting≠commit-applied, quorum can include non-members (P0-2); quorum floor wrong axis — empty/self-only views are provably det-pure while two different quorum views still diverge (P1-3, with the alternative); 10Hz full-index-scan retry loop (P1-4); self pads quorum on drain terms (P2-5).
- Rounds 2-4 (b0f8dc7): det-DEGRADE replaces the hold — admit_exchange_completion tri-state (Admit / AdmitDetOnly=emptied view→pure det table, node keeps serving / HoldReheal=#73 unchanged); member-filtered quorum numerator; degraded-term UPGRADE (racing second exchange's quorum view passes the dup gate; gated on active_count()==0 — the same-epoch plan supersede would unfence live workers (R3-1)); event-repair arm skipped on empty view; retained view deliberately blinded on degrade; residuals NAMED in-code (det-stands-for-term if no quorum view ever arrives; asymmetric-reachability quorum-view divergence pre-existing; hysteresis reset flag-on). Round 4 APPROVE. Task #79 filed (first-activation branch lacks committed-topology applicability check, pre-existing).
- Wedged test now green in 4s. Full gate green (cli_log_level_set + partitioned_minority flakes verified green standalone). Pushed 51b9061..b0f8dc7. Runs dispatched: default 31971906387, armed 31971908443. Worktrees cleaned (removed=4).
## Wave 9 in flight (2026-08-17) — runs @ b0f8dc7: default 9/14, armed 7/14; 11 green BOTH, 08-default + 10-armed newly green
- Diagnosis (4 agents, all landed): 09 = det-degrade Residual #1 live (node2 degraded term 6, upgrade lost the 7s commit-propagation race, det table never re-arms; 1451 same-version overlaps) + single-shot exchange is the root (06: all nodes degraded the 4-member term → holder-blind 5900-move plan → 57% write outage; degraded plan locks out upgrade via active_count()==0). 05a/08a/15/17 = compensation-tombstone family (compensate_replication_failure writes ClientDelete gen=0 → RULE-DS permanently vetoes the live copy → terminal abort → orphan cleanup ate the last copy = ACKED LOSS 3/5500, 1/1200). armed-17 242 over-RF = orphan cleanup driverless. armed-06 = code-22 manifest-mismatch livelock. 07 = harness removes node4 mid-backfill (drain itself FIXED); default-15 = per-IP cap 64 (gateway aggregation).
- Fixers: w9smalls MERGED (f8fb1e7; review APPROVE after P1 unreachable-mechanism reword + P2 version-scoped divergence clock; survivor-side NodeLeft theory = task #81). w9exchange DONE awaiting bitcoin-expert (re-query 500ms; degraded retry arm 2s→30s no-slot; DeferredPlanLaunch 3s grace + active_migrations_all_held exact-set gate; also fixed vacuous cluster_delayed_activation). w9repair DONE awaiting code-review (code-22 escalation via stream_shard_baseline bounded 3; event-driven orphan cleanup 60s + retained-no-evidence gauge; repark_departed_source_inbound committed-membership; LOST-witness exclusion). w9tombstone fixer STILL RUNNING (CompensatedCreate cause + live-copy superiority + replica-abort resync signal; bitcoin-expert round mandatory when done).
- Next: land reviews → merge w9exchange/w9repair/w9tombstone → full gate → push → both runs → cleanup-worktrees.
## Wave 9 SHIPPED (2026-08-17) — pushed b0f8dc7..d3437e4, runs 32010681108 (default) + 32010683878 (armed) validating
- ALL FOUR branches merged after review APPROVE: w9smalls (f8fb1e7, 1 review round), w9repair (78f8edb, APPROVE_WITH_NITS → task #82), w9exchange (f6f47b2, expert REQUEST_CHANGES→APPROVE: quorum early-return in run_exchange_phase, duplicate_completion_upgrade_applicable stale-term gate, 5s grace re-derived + relation-pinned, stale-epoch reply echoes responder key w/ higher-key fast-stop, mark_failed_exact 4-tuple cancel was a REAL bug, 300s retry ceiling), w9tombstone (d3437e4 w/ conflict resolution + J2 doc reword eb398fe, expert REQUEST_CHANGES→APPROVE: PruneReplace now a real generation-gated cause for the #29 prune + replace-duplicate strand (the THIRD ClientDelete producer the expert found — CI signature could have recurred), CompensatedCreate >= vs PruneReplace > asymmetry ratified with rationale, weak-vs-weak first-claim-stands, no-wire-variant verified unreachable, Part B scoped to keeps-mastership + observe_forced remainder + committed flag replica_abort_forced_resync_enabled default TRUE).
- Merge seams: dispatch.rs/migration.rs/cluster_delayed_activation.rs both-adds resolved keep-both (+1 shared closing brace each in migration.rs and dispatch.rs). Full gate green: suite 0 FAILED (3 grep hits = injected-failure tracing fixtures), clippy clean BOTH feature sets (fault-injection sweep now in the gate — CI gap the tombstone round exposed), fmt, client crate.
- Worktrees cleaned (removed=4). Expectations: 09/06 via exchange trio (re-query + degraded retry + grace), 15/17/05a/08a via tombstone-currency + forced resync, 07 via harness quiescence wait, armed-06 via code-22 escalation, default-15 via per-IP cap 256.
- LESSON (twice this campaign): never pipe gate output through head/truncating filters — re-verify with a failures-only grep + exit code.
## Wave 10 dispatched (2026-08-17) — wave-9 validation REGRESSED (7/14 both runs @ d3437e4), full triage done
- Harness fail-fast DEMOTED to timeout annotation (730e036): it killed 5 scenarios (incl. durably-green 11) at ~5s inside converging windows — emergent cross-branch defect (upgrade re-activates at SAME version; /status.shard_table_version is the COMMITTED term by design, so the detector compared targets across the commit-vs-activation gap). PROCESS FAILURE: no composition review over the 4 wave-9 branches.
- TRIAGE (2 diagnosers): THE ROOT OF ROOTS = O(records) per-key device-read scan on the exchange critical path (build_self_partition_version_entries → recency_for_keys, doc'd off-hot-path; runs in every report handler + before the exchange deadline starts; frame-read timeout 2s == exchange window 2s → every re-query structurally attempts:1) — starved ALL wave-9 exchange machinery (06/07/09 walls; the degraded upgrade DID work in vivo once handlers answered — sc05 08:42:48). ACKED-LOSS shapes: armed-15 36 lost = quorum early-return let 3 nodes activate one epoch from 3 DIFFERENT 2-of-3 views → divergent refined tables → 35 ownerless shards → divergence-unaware cleanup destroyed copies; armed-05 5 zero-holder = PRE-EXISTING #29 prune race (manifest folded before concurrent create → prune deletes the acked copy) now STRANDED by PruneReplace gen-0 veto (created-once records are gen-0 forever → "strictly newer" vacuous) + tombstoned holder claimed authoritative-complete; armed-11 = event orphan-cleanup deleted a rejoining node's copies 2s before the completion-only already-serving handshake handed shards back EMPTY (transient). CROSS-CUTTING P0 (found 3× independently): synthesized resync tasks never registered → completions dropped as untracked → repairs silently never commit. 07 outage: bogus term committed WITH the removed member (stale SWIM alive-set at propose) + activation starvation + GET rejects DEBUG-only. NO WHOLESALE REVERT warranted.
- Fixers: w10exchange (O(RAM) report + timeout decouple + FULL-view-only refinement + GET WARNs), w10repair (register resyncs via start_outbound + already-serving presence check + defer rejoin-boot event cleanup), w10prune (prune enumeration-cutoff race close + weak-veto arbitration under fence + weak-holder can't claim authoritative-complete + proposer member-set revalidation). Expert rounds to follow on exchange + prune.
## Wave 10 checkpoint (2026-08-17, mid-review-loops)
- w10repair: rounds 1-3 — APPROVE pending round-3 fix (demote-with-EMPTY-key-list so the empty path's skip-aware fenced recheck owns the 0<M<N shape; reviewer caught readable-subset stream-and-commit = acked loss on the streaming path). Commits eb27c86/b690a41/af25f6f/4674823/a7c3015 + round-3 in flight.
- w10exchange: expert REQUEST_CHANGES — P0 fabricated emptiness (unrefreshed cache serves empty fingerprint for data-bearing node → reverse-heal direction gate self-fences every mastered shard on reboot → alert-and-hold; fix = PARTITION_FLAG_RECENCY_UNKNOWN, unknown ⇒ no suspect/no direction, heal-source ignores unknown max_gen, real-builder red tests) + P1 unbounded back-to-back whole-store scans (min refresh interval + metrics) + P1 spawn-failure slot orphan + 9 P2s (incl. parked-fence re-source must run on PRE-clear view under the full-view floor). Fix round in flight (commits ec6da92..6dbf3b6 + fixes).
- w10prune: first pass still running (prune race cutoff, weak-veto arbitration, authoritative-complete exclusion, proposer member-set revalidation).
- REMAINING after all three land: expert re-verifies (exchange, prune), THEN the wave-10 COMPOSITION REVIEW (cross-branch, the wave-9 lesson — esp. tracked-resyncs × degraded-upgrade gate, recency-UNKNOWN × heal-source selection, prune-cutoff × escalation), merge all three, full gate (no truncating filters!), push 730e036.., dispatch both nightly runs, cleanup-worktrees, analyze.
## Wave 10 progress (2026-08-17): 2 of 3 merged
- w10repair MERGED (f30be61) after 3 review rounds → APPROVE. Landed: Phase-H resyncs registered via start_outbound (closes the silent-no-op repair; deliberately reverses the W9 "Phase-H untracked" exception so streams now correctly gate the degraded upgrade), already-serving completion verifies target holds data (skip-aware enumeration; ANY skipped>0 demotes with an EMPTY key list so the empty path's fenced recheck owns it), empty-no-churn-manifest-over-live-shard guard (fixer found this deeper hole itself; narrowed from len<live to EMPTY-only after stability runs disproved the broad form), per-task W3-FIX-C table disposition at all four fail sites. Reviewer caught a real acked-loss residual mid-round (readable-subset stream-and-commit).
- w10exchange MERGED (8d60e28) after 2 expert rounds → APPROVE. Landed: O(RAM) partition-version report via ShardRecencyCache (5s paced, off-thread, Builder::spawn + inline slot release), 600ms report reads decoupled from the 2s window, FULL-member-view floor for ALL refinement (closes the armed-15 divergence door), and — the expert's P0 — PARTITION_FLAG_RECENCY_UNKNOWN: an unrefreshed cache would have served the EMPTY fingerprint for a data-bearing node, making every rebooting node self-fence its whole mastered keyspace via the reverse-heal direction gate (fabricated emptiness, F1 shape). live_count==0 resolves KNOWN-empty (expert ratified as better than his own prescription: keeps a genuinely-emptied master detectable). Residuals ledgered #84.
- w10prune: expert REQUEST_CHANGES → fix round in flight. P1: the enumeration-cutoff bound breaks on a receiver watermark REGRESSION (missing tracker file = empty; sender's last_acked never lowers; Gap relabels DOWN below the stale cutoff → prune deletes the live acked copy → armed-05 re-arms). Fix = target-side fail-safe: cutoff above our own watermark ⇒ source's view is stale ⇒ skip. Plus 6 P2s (stale weak-key snapshot across retries, O(all tombstones) scan, cross-source premise doc, silent fallback, arbitration behind a default-TRUE flag, prune-cleanup residual). Expert RATIFIED the always-on arbitration posture (it never authorizes deletion — the handler applies no record; clear_weak is cause-gated atomically under the shard lock) and found NO new loss path in arbitration × FIX-3 composition.
- NEXT: w10prune re-verify → COMPOSITION REVIEW across all three (the wave-9 lesson) → full gate → push → both runs.
## Wave 10 all merged @ bc1b4e5 — COMPOSITION REVIEW earned its keep (4 P1s)
- w10prune MERGED (bc1b4e5) after 3 expert rounds. P1 closed: the enumeration cutoff broke on a receiver watermark REGRESSION (missing tracker file = empty; sender's last_acked never lowers; Gap relabels fresh content DOWN below the stale cutoff) → target-side EQUALITY gate (watermark == cutoff); expert ratified the equality collapse for a stronger reason than given (equality ⊂ <=, so it can only REMOVE permissions; leg 2 already dominated liveness). Plus per-send weak-key recapture, O(weak) accelerator, flag default TRUE, docs/metrics.
- FULL GATE GREEN on the merged state: 81 test binaries 0 failures, clippy both feature sets, fmt, client crate.
- COMPOSITION REVIEW (the wave-9 lesson applied): APPROVE would have been wrong — 4 P1s, all pure-composition, none visible to any single-branch review. ROOT: registering Phase-H resyncs via start_outbound silently enrolled them in FOUR subsystems never designed for them. (P1-1) resyncs now reach 5+ blanket-Rollback fail sites W10 fixed only 4 of → a failing replica resync reverts a concurrent mid-Copying master handoff → DUAL-SERVING MASTER. (P1-2) start_outbound's dual_write_add + the R2/P7b handoff-ACK gate = a resync toward an existing replica makes that replica's ACK mandatory for every client write to the shard; a shards:[] NeedsResync expands to ALL its shards → repair becomes a cluster-wide write outage (inert at RF=2+WriteAll, bites at RF≥3). (P1-3) the cached recency's skew is SYSTEMATICALLY DIRECTIONAL (self serves pre-kick snapshot; re-queried peers serve newer) → on spend-heavy load the master reads "replica ahead" → FENCES ITS OWN SHARDS (the sc07 read-outage shape, re-created by its own fix) — and each fence is a heal_pending entry that permanently disables B's new inbound_count()==0 cleanup gate. (P1-4) the already-serving probe is serial+un-pooled ahead of chunking while active_count>0 parks all five convergence gates.
- 12 seams CLEARED explicitly (incl. prune-cutoff × tracked-resyncs — resync streams are !tracked so they cannot move the gate; lock ordering; arbitration × UNKNOWN recency; wire compat).
- Fixers dispatched: w10cresync (P1-1/P1-2/P1-4/P2-9), w10crecency (P1-3 both halves, P2-1 joining-node drop, P2-6 weak-declaration past GC horizon).
## Wave 10 SHIPPED @ 3a38dc2 — runs 32055073890 (default) + 32055076441 (armed): 7/14 both, COMPOSITION CHANGED
- All five branches merged (w10exchange 8d60e28, w10repair f30be61, w10prune bc1b4e5, w10cresync bb6409f, w10crecency 35b29c1) + merge-seam test-lock fix 3a38dc2. Full gate green (3438 lib tests ×3 consecutive stable runs, clippy both feature sets, fmt, client).
- COMPOSITION REVIEW earned its keep: 4 P1s no single-branch review could see (all from start_outbound registration reaching 4 subsystems) + 12 seams explicitly cleared. Both composition fixers then went through their own review loops (resync APPROVE after 3 P2s; recency APPROVE after P1 cap + ownership filter + rotating cursor).
- MERGE-SEAM FLAKE caught by the final gate: migration_active is a process-global gauge and the new bulk-registration tests (515 start_outbound calls) raced the gauge test's before/after reads. Fixed with a shared migration_metrics_test_lock taken on BOTH sides (asserting test + the two perturbing tests); pinned by 3 consecutive clean full-suite runs. LESSON: a test that reads a process-global metric across an interval is only as stable as the loudest neighbour.
- RESULTS: 7/14 both (same score, different composition). BIG WINS: default-09 divergence GONE (was 1451 overlapping same-version targets → now masters=4096/4096, handoffs=0, activation=ok, exact targets, only inbound=2 stuck); default-05 5-record zero-holder loss → 24 overlaps + 26 handoffs (convergence tail); 11 green BOTH; armed-09 green. STILL RED: 06 both (still divergent at the JOIN — targets sum 4408 at ver=2, node3 carrying what looks like a 3-member table while others moved to 4, 991 overlapping, 76.4s persistence — the demoted divergence annotation is now doing its job), 07 both (default: node4 drains fully but n1/n2 hold ~1900 active migrations that never settle; armed: rs:430 total read-back outage), 15/17 census both, 08 both, armed-04 NEW (previously green — likely wave-10 regression, prime suspects: E's fence confirm on the event loop, C's tracked resyncs holding active_count>0 blocking post-kill reactivation, D's deferral timing).
- Two diagnosers dispatched (06/07 convergence+divergence; tails 09/05/15/17 + the armed-04 regression, lead item).
## Wave 11 dispatched (2026-08-17) — two diagnosers decomposed the 3a38dc2 runs; 2 GENUINE wave-10 regressions found
- THE BIG REFRAME: 06 and 07 are ONE bug, and wave-10's recency fix (A) WORKED — that is what unmasked it. Full views now arrive, so apply_master_election deviates ~1000-1250 masterships mid-rebalance (the det master is empty BECAUSE THIS ACTIVATION'S PLAN FILLS IT; the adopt_view_holders gate blocks only EXTERNAL holders, the assignment-internal replica promotion is ungated); the deviation counters bill every one as damage (third_party_deviation arms the re-heal on EVERY node); the re-heal fires 41-65ms after a node goes idle; and add_local_holder_backfill_tasks takes NO partition_view so every plan re-streams the WHOLE STORE (07 round-2: outbound 1921 against SEVEN real master moves; streamed records == store exactly). Two waves don't fit 120s. The re-heal's answer is TIME-DEPENDENT (deviations decay to det once migration fills the masters), so staggered re-heals install different tables at the SAME version = the 06 "divergence". NATURAL CONTROL in the same commit: armed-06 re-healed within 2.8s on all four nodes → targets summed 4097, no divergence (failed on raw slowness instead).
- WAVE-10 VERDICTS in vivo: A worked (exchanges 83-200ms, full views, zero degrades) and is a genuine fix; B (full-view floor) never fired in 06/07-default, fired correctly in 07-armed where it AMPLIFIED a bad member set into an unservable det table; C/D/E essentially inert (D exonerated by four independent zero-counts). NO wave-10 change caused 06/07.
- TWO GENUINE REGRESSIONS: (1) eb27c86 — orphan-cleanup gate ⇄ inbound entry CIRCULAR WAIT (default-09 red: everything converged, inbound=2 forever; the entry is kept because records exist, the records need cleanup, cleanup is gated on pending_inbound==0); (2) 6c39025 — the cutoff gate's "always a deferral, never a wedge" doc is FALSE under exact_entries_verified (superset accept commits WITH the extras; ERR_MIGRATION_IN_PROGRESS appears 0 times in 5791 lines despite 1812 refusals). Plus b690a41 AMPLIFIES the pre-existing under-replication sweep non-convergence (registered resyncs hold active_count>0 → block the reactivation that is the ONLY thing that refreshes the retained view the sweep derives from; 8 passes/13s → 33 passes/64s), which is what fails armed-04's non-retrying Test 4.5.
- MORE WAVE-10 WINS CONFIRMED: armed-15 went from iteration-1 failure with 36 lost records + divergent tables → EIGHT iterations, zero mismatches, targets exact (now fails only on a harness client-leak/per-IP-cap); default-09 divergence gone; default-05 zero-holder loss gone.
- Four fixers: w11oscillation (view-gate the backfill; suppress refinement when this plan moves the data; re-heal install agreed-or-noop), w11membership (catch_up_fallback_proposal must never widen membership — it sources members from the ADDRESS BOOK and resurrected a quiesced node into term 4, putting 1024 shards on a dead node; apply-commit-before-broadcast; narrow the total-outage route gate; client refuses is_alive=0), w11regress (the two regressions + sweep non-convergence + resync fence origin + code=37 retry + negative transfer reply + metering), w11harness (per-IP cap missing from EVERY generated config; Client has no Drop so refresh tasks detach and re-dial forever; 4.5 retry; armed diag capture gap).
## Wave 11 SHIPPED @ fc5e5f7 — pushed 3a38dc2..fc5e5f7, runs 32074444919 (default) + 32074447561 (armed) validating
- ALL FOUR branches merged after full review loops: w11harness (fe37f34), w11oscillation (c4243a9), w11regress (4c970c1), w11membership (fc5e5f7), + docs/metric corrections (0789e2a, e1d9197). Full gate green on the composed state: 81 binaries 0 failures, clippy BOTH feature sets, fmt, client crate. Worktrees cleaned.
- THE WAVE'S CENTRAL FINDING: wave-10's recency fix WORKED, and that is what unmasked the real defect. 06 and 07 are ONE bug — with full views finally arriving, apply_master_election deviates ~1000-1250 masterships mid-rebalance (the det master is empty BECAUSE THIS ACTIVATION'S PLAN FILLS IT), the counters bill every deviation as damage, the re-heal fires 41-65ms after idle and reverts to det, and add_local_holder_backfill_tasks re-streams the WHOLE STORE per plan. Two waves don't fit 120s; staggered re-heals install different tables at the SAME version = the 06 "divergence". Natural control in the same commit: armed-06 re-healed within 2.8s on all four nodes → no divergence.
- FOUR TIMES the danger was in a FIX rather than the original defect, and every one was caught in review: (1) oscillation's re-heal decline was TERMINAL for a settled member node → permanent master split with a DUAL-AUTHORITY sub-case (double-spend window) — bounded to 2 declines; (2) membership's catch-up repair (MY prescription) unioned a stale committed set with a fresh one, re-adding a quiesced-but-ALIVE node and UNDOING A GRACEFUL DRAIN — replaced by direct adoption under a configured cluster_id; (3) that fix's liveness filter then excluded `== Alive` so Suspect AND None folded back in (absence of an ACK read as licence to re-add) — one token, `!= Some(Dead)`, positive proof of death; (4) regress's superset-refusal would have fired in the STEADY STATE of the very repair traffic it was meant to protect, with an abort-not-retry disposition — DROPPED after the fixer PROVED the prescribed retry cannot converge (the gate compares a STREAM-WIDE watermark for exact equality, so any write from that source on any shard invalidates a fresh fold).
- REVIEWERS RETRACTED THEIR OWN PRESCRIPTIONS TWICE ("My P1-1 recommendation was wrong on this point; dropping is correct"), and a pinned safety test was REVERSED deliberately after verification: monotonicity as a PROPOSAL gate prevents creating the fact; as an ADOPTION gate it refuses to learn a fact that already happened, and refusing to learn it does not undo it. Containment verified at 5 mint/attest sites — the shape can be adopted, never manufactured.
- CAMPAIGN-LEVEL DEFECTS FOUND: scenario panics bypass BOTH diagnostics capture and teardown (#86); DockerHelpers::collect_logs read STDOUT ONLY while docker writes traces to STDERR, so every log it ever wrote was near-empty (#88, fixed); git stash is REPO-WIDE so a pop in an agent worktree takes a foreign entry (now a CLAUDE.md rule, 0789e2a); global test metrics make gauge assertions structurally flaky (#87).
- Expectations for these runs: 06/07 wall-clock (the whole-store re-stream is gone on SETTLED rounds — #91 calibrates whether it partially returns under load); 09's two dangling inbounds closed by the negative transfer reply; armed-04's sweep now converges and resync no longer write-fences; armed-15's client leak + per-IP cap fixed; 07-armed's resurrection cascade closed at the source. NOTE FIX 3 (partial serving) ships INERT behind a default-off flag pending #89.
## Wave 12 SHIPPED @ 0e00822 — pushed fc5e5f7..0e00822, both runs validating
- Wave 11's results were the campaign high-water: default 11/14 (prev best 9), armed 9/14 (prev best 8). 04/05/07/09-default/10/11/14 green. Every remaining failure a TAIL: 06 error rate 57.28% → 3.75%/5.67%; armed-09 overlapping 1451 → 2; armed-08 fully converged except inbound=2; 17 over-replication 619 → 192 (all at 3 holders, zero loss).
- WAVE 12 = two diagnose-then-fix agents, both through full review rounds.
- OVER-REPLICATION (1fe1c23): the wave-11 METERING immediately paid off — orphan_cleanup_retained_no_evidence was the gate, the retain series was FLAT over 91s (1008→146→146→135→135), and the arithmetic closed (43 shards × 1.035 ≈ 45 reported; 158 × 1.264 ≈ 200 vs 192). Not a driver gap, not a census artifact: the #28 guard correctly refusing with NO WAY TO EVER EARN THE PROOF (committed_handoffs is in-RAM and epoch-exact, so a SIGKILLed node can never earn evidence for handoffs it never performed) — while the census showed holders=[0,1,2], i.e. the proof was available but unaskable. FIX: do NOT relax #28; give the pass a way to EARN it — `confirm_target_holds_superset` already existed as exactly the right primitive (verify-only, needs no inbound entry/fence/prior handoff from the asker), no wire change. Unanimous confirmation from every committed holder + generation guard + post-probe re-verify.
  REVIEW CAUGHT (data-deletion path, so it was attacked hard): the anti-mutual-reclaim invariant was DELEGATED ENTIRELY TO THE PEER and disabled at epoch 0 → now enforced ASKER-SIDE (responder echoes its version, attests() checks it) which is strictly better than prescribed; probe phase STARVED ITSELF (no rotation cursor + no failed-holder memo → one hung holder burns the 5s budget and the tail is never probed) → cursor + memo; it RE-INTRODUCED the unbounded per-shard index walk W10 had already bounded; a vacuous-unanimity path via ADDRESS COLLISION (two ids → one address = one answer satisfying "unanimous") → node-id echo binding; and the durability fix was HALF A BARRIER — flush_all_redo syncs redo only, but under buffered durability the create redo is index-only CreateV2 whose bytes live on the NOT-YET-SYNCED data device, so the attestation was cryptographically well-bound to a claim that still wasn't durable (ensure_local_write_durable, one module over, already did both and its comment named the exact failure).
- THE TAILS (0e00822): 06 was NOT harness miscalibration — a genuine SHIPPED-CLIENT defect: every sampled error was NO_QUORUM(15) in a 100-195ms commit-vs-activation window, the server marks 15 retryable at all three emit sites and the harness's own retry.rs listed it, but the Rust client's is_retryable_error_code did NOT — so a 1-item batch got ONE zero-backoff retry inside the same window. armed-08: FIX 4(a) DID fire (29 requests, 29 refusals) but the drop was vetoed because the node still holds records — a genuine fixpoint, reclassified-not-repaired with the boundary respected. armed-09: weak-veto arbitration is UNSATISFIABLE FOR A PHANTOM SOURCE (the authority proof requires the target's table to credit the source; a phantom never can) — livelock bounded, root tracked as #93 rather than widening authority blind.
  REVIEW CAUGHT: a SELF-INFLICTED COMPOSITION DEFECT — Change 3 rewrites assignments without bumping the version, and Change 2 then marks the resulting refusal terminal and teaches the CI gate to ignore it, so the harness would declare convergence on a genuinely fenced shard (the ONE entry class that needs the gate) → tri-state InboundRetention{Drop,KeepHolder,KeepOrphan}, mark only KeepOrphan; and A GO CLIENT — Teranode's actual consumer — still hard-failing the same shape with ZERO retries and a test PINNING the contradicted contract → ported, not diverged.
- Gate green on all four crates incl. Go. Worktrees cleaned. Residuals tracked: #93 (phantom arbitration authority), #94 (superset proof compares generations not content — a diverged same-generation record reads as contained), plus the oversized-shard limit (at 2B records a whole-shard manifest exceeds MAX_FRAME_SIZE, so those shards need a ranged/digest mechanism — now METERED rather than silent).
- PROCESS NOTE: reviewers RETRACTED their own prescriptions twice more this wave, a fixer proved a prescribed retry CANNOT converge (stream-wide watermark) and dropped the change instead, and a fixer re-split its own history when one commit's message no longer described its contents.
## Wave 13 MERGED @ e7ebab4 — DATA-LOSS CONTAINMENT + veto wedge. NOT PUSHED (push blocked by permission classifier; awaiting user)
- WAVE 12 SHIPPED A DATA-LOSS DEFECT AND CI CAUGHT IT. Run 32084447959 default-15.3: four ACKED records read back `deleted (NotFound)`. The proof-of-elsewhere reclaim (1fe1c23) deleted them. THE PATH LOGGED ONLY AT DEBUG AND SHARD-GATED, so there was NO log line at all — attribution had to be reconstructed from freed-space arithmetic (node1: 96 logged deletions vs 100 dead records, gap 4 = proof_reclaimed 4; node2: gap 23 = 23; two independent exact matches) plus the census gauge (`shards:13 SKIPPED unsettled_task:8` -> 300ms later `shards:13 unsettled_task:0`; candidate set 17, reclaimed 4, 17-4=13). The four missing txids derive to shards 1094/974/758/1439 — EXACTLY the four shards node1 had refused to hand off seconds earlier.
- ROOT CAUSE, and it is NOT #94's generation-vs-content blindness (explicitly ruled out — create-only gen-0 records, nothing mutated): **UNANIMOUS CONFIRMATION IS NOT A RETENTION PROMISE.** The probe attests presence at an INSTANT; there is no lease and no commitment to keep the copy. Two independent holes: (1) a confirming holder can be draining the same copy through the LEGACY path (has_committed_handoff -> cleanup_orphaned_shard_if_settled); the two reclaim paths are mutually blind, and node2 legacy-reclaimed its copies within ~100ms of confirming them. (2) The #46 guard's refusal ("no-churn manifest EMPTY while the shard live-holds records; source keeps authority") lives ONLY as a `Failed` entry in active_migrations(); run_orphan_cleanup skips unsettled tasks, so once the failed task is RETIRED the proof pass sees a plain non-owned shard and deletes exactly what the guard just refused to hand over.
- CONTAINMENT (fix/w13gate, 4 commits): flag `orphan_cleanup_proof_reclaim_enabled` default FALSE at one choke point inside run_orphan_cleanup (NOT at the two PeerSupersetProof construction sites, so a future call site cannot re-arm); disarmed = byte-for-byte the pre-1fe1c23 #28 posture, nothing probed, nothing folded, nothing deleted. Reclaim now logs at INFO with shard/count/confirmed_by/epoch/txids (capped 16 + txids_omitted). Review VERIFIED all five claims by trace; `ClusterConfig` has no Default impl so all 9 sites are compile-enforced.
- ATTRIBUTION DISCIPLINE PAID OFF TWICE. 17.3's six under-replicated were NOT the reclaim — census self-consistent with ZERO loss (550*3 + 6*1 + 1044*2 = 3744 = node totals); those copies were never CREATED because heals into them were vetoed. **Deleted and never-created look IDENTICAL in a final census; only the logs separate them.** And my own #93 attribution for default-08 was WRONG — computing the real placement (placement_version 2 -> HRW, RF=2) shows shard 1561 at epoch 4 is master=n3 replicas=[n2] and the refused requester was n1, holding no part of it. Refusing was the gate WORKING. Real 08 cause tracked as #95 (no holder-driven repair driver; the armed sweep supplies it, which is why armed-08 passes).
- VETO WEDGE (fix/w13veto, 4 commits): `blocks_heal_apply` gates PruneReplace with `generation_at_or_ahead(tomb_gen, incoming_gen)`; for a created-once record BOTH are 0 FOREVER, so the "strictly-newer heals in" escape is structurally VACUOUS and the veto never expires. The designed remedy (weak-veto arbitration) was refused by its own authority gate. Fixed by widening to HOLDERS across the in-flight activation + veto TICKETS + suspend-don't-delete.
- THE REVIEWS' CONVERGENT FINDING (bitcoin-expert + security-auditor, INDEPENDENTLY): removing the inbound fence removed a **CONTEXT** property, not an identity one. The fence was forgeable as identity but could only EXIST while the shard was Copying/inbound-expected — i.e. client-INVISIBLE. In steady state it is not raisable at all. So the first cut let a credentialed peer clear anti-resurrection markers on a LIVE, CLIENT-SERVING shard. And the worse direction is LOSS, not resurrection: the weak marker is the node's only proof that a key missing from the manifest it SHIPS is its own prune damage rather than deletion-intent, so clearing it without the re-push landing arms the peer's #29 prune to delete the LAST LIVE COPY (tombstone.rs already documents this as why weak causes are GC-exempt).
- FIXER REJECTED TWO OF MY PRESCRIPTIONS, BOTH CORRECTLY, BOTH WITH PROOF. (a) The proposed narrowing to prev's MASTER: classified all 107 refused (source,shard) pairs — `PREV: master=0 replica=107`, `CURR: neither=107` — it would have fixed NOTHING, and it does not close its own P0 scenario (that attacker is a CURRENT replica). When the reviewer challenged the classification on the grounds that replica_migration_plan prefers the old master, the fixer ran the planner over the real tables: 0/107 refused pairs appear in that plan AT ALL (2 old members at RF=2 -> both already hold every shard -> the skip fires for all; these are a third `backfill` category). (b) My "make clear+apply atomic via a wire change" -> replaced with `suspend_weak_veto`: a RAM-only flag lifts the RULE-DS veto while the marker STAYS in the log and STAYS in weak_tombstone_keys, so the DECLARATION survives and the loss chain never opens. Smaller, no wire change, non-durable by design (restart re-arms, the conservative direction). Re-review attacked it end-to-end and it held.
- RE-REVIEW ALSO CAUGHT: the veto TICKET is minted from a SELF-ASSERTED `completion_from_node` — a frame-capable peer sends a completion that gets refused (minting the ticket), then arbitrates. Real replay defence and real accident-containment; roughly NO hardening against anyone holding the cluster secret, and in trusted-overlay mode (the E2E config) against any TCP client. Three code comments claimed otherwise and were corrected rather than left. Fixed better than prescribed: ticket scoped to the CONNECTION (producer already sends both frames on one stream), which DELETED the global store, TTL, cap, eviction and four accessors. Also caught a test passing at the WRONG gate (`a_ticketed_replica_requester_still_cannot_clear_a_client_delete_marker` never held a ticket, so it was refused at the ticket gate, not the cause gate — delete the cause check and it stayed green).
- **A BLOCKING CI GATE WAS RED THE WHOLE CAMPAIGN AND NOBODY NOTICED.** `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --all` (ci.yml:50-57) failed with 6 unresolved intra-doc links inherited from this branch chain; origin/main is green. The first gate review MISSED it because it ran plain `cargo doc`, where these are warnings not errors. Every push this campaign made would have failed it. All fixed; verified exit 0 on the merged tree. ADD THIS TO THE PRE-PUSH GATE SET.
- MERGE: 4 conflicts, all pure doc-text (both branches independently fixed the same links). One was a FACTUAL disagreement — `Self::start_outbound` vs `Self::start_outbound_resync` for the sentinel-replacement referent; resolved by reading the code (the replacement is in the private start_outbound_with_origin; both public entries delegate; the comment says "forward-migration", so start_outbound).
- GATE ON MERGED TREE: cargo test --all 4054 passed / 0 failed / 0 ignored; clippy --all-targets -D warnings clean; fmt clean; RUSTDOC clean; client/rust 70+36+6 passed.
- RESIDUALS TRACKED, NOT ABSORBED INTO COMMENTS: #94 (rewritten with the real root cause + the redesign options: responder-side refusal / durable refusal / transfer-then-relinquish), #95 (default-08 holder-driven repair driver), #96 (absent-key delete records NO tombstone — `delete_inner` bails before `log.record`, `receiver.rs` swallows TxNotFound — so a weak marker can PROXY another node's ClientDelete; this is the true root of the resurrection exposure and the one thing standing between the mechanism and a real resurrection), #97 (per-peer identity in ConnectionState; until then `from_node` is a claim, not an identity, and every gate built on it inherits that).
- PROCESS NOTE: the reclaim's DEBUG-only logging turned a 5-minute grep into an hour of forensic arithmetic. A path that deletes data must never be silent — now enforced at INFO. And the containment carries a documented TRAP at BOTH the flag and the harness assertion: leaving it off knowingly re-reds assert_rf2_replication_exact on 15/17, the red is the SAFE state, and the obvious "fix the CI red" move is to arm the configuration that destroyed four acked records.
## Wave 14 (in progress) — CI 11/14 default @ c444734, then TWO data-loss root causes
- WAVE 13 VALIDATED: runs 32630545533 (default 11/14, TIES the campaign record) and 32630553766 (armed 10/14). **The veto-wedge fix converged what it targeted**: default-09 red->green and armed-05 GREEN in both (armed-05 was the 107-terminally-aborted-handoff case). 15/17 red BY DESIGN — 3 of 4 are PURE over-replication ([3:49], [3:413], [3:84], zero loss) and the TRAP note fired in all four.
- MY #93 ATTRIBUTION FOR DEFAULT-08 WAS WRONG and the fixer proved it: computing the real placement (placement_version 2 -> HRW, RF=2) shows shard 1561 at epoch 4 is master=n3 replicas=[n2], and the refused requester was n1 — holding no part of it. Refusing was the AUTHORITY GATE WORKING. All 9 refusals share that shape.
- #95 DEFAULT-08 — the sweep was not idle, it was **FENCED BLIND**. Verified link by link by the reviewer: `retained_exchange_view` has exactly ONE writer (exchange completion); `should_trigger_topology_reactivation` requires `mismatched_shards > 0 || pending_handoffs > 0`; under-replication contributes to NEITHER (all six `repair_shards` inputs are table-vs-committed comparisons that never look at holdings). So on a settled cluster the view FREEZES and the freshness fence CORRECTLY refuses to read its stale replica-zeros as absence of data. No arming cadence could ever fix it — the driver wasn't failing to run, it was running against evidence it was right to distrust. FIX: the master ASKS (read-only `UnderReplicationProbe` re-runs `run_exchange_phase` on a 15s cadence). Default-ON, on two legs: the failing config IS the default, and the fire path is already default-reachable via `replica_abort_forced_resync_enabled`. **[SUPERSEDED in W15 — the default is now OFF.** A three-way A/B at 7573f07 measured the driver FIXING 08 and CAUSING 11's election divergence (`masters=4094/4096`) and correlating with armed-05 zero-holder loss: 32637568483 (default, repair ON) 08 PASS / 11 FAIL vs 32639946439 (default, repair OFF) 08 FAIL / 11 PASS; 32637576348 (armed sweep, repair ON) 05 FAIL vs 32639877944 (armed sweep, repair OFF) 05 PASS. Create-only in isolation, NOT under composition: the fills ride full-shard migrations whose `OP_MIGRATION_COMPLETE` runs the target-side `#29` prune with the master authoritative-complete, and `prune_safe_at_cutoff` is scoped to THAT source's stream, so keys the target took from another source after the (<=15s stale) probe view get deleted. Ships inert behind the same flag; re-arm only once driver fills are non-authoritative-for-prune or the cutoff proof spans sources.**
  REVIEW CAUGHT: the probe turned a whole-store index+device scan from event-driven into CONTINUOUS on every node — `run_exchange_phase:8374` and `dispatch.rs:14394` both kick `maybe_refresh_shard_recency_cache`, braked only by a 5s floor, and on a write-busy store `note_mutation` keeps it permanently stale so trigger rate == scan rate; at N>=4 the peer kicks alone saturate the floor. `recency.rs:565` already calls back-to-back whole-store scans "the P1-1 defect". Fixed with an additive origin byte skipping the refresh AT BOTH ENDS (a querier-only fix leaves the N-1 amplification intact).
- **ARMED-09 WAS NOT MY REGRESSION** — proven at file level (the wave touched none of recovery/allocator/segment_allocator/redo/checkpoint/index/io/replication; its only engine.rs change was a 36-line rename) and by mechanism (`is_subset_master`'s range-safety is behaviourally identical for in-range shards; the new authority predicate fired 3x and SUCCEEDED every time). Root cause was #69.
- #69 CONFIRMED DATA LOSS with exact accounting: `delete_inner` zeroed the record's metadata header (BUFFERED, un-journaled, unsynced) BEFORE the fsynced FreeRegion that is the delete's ONLY durable commit. Crash between them = destruction lands, commit doesn't; boot restores the index entry, nothing evicts it, and the entry survives pointing at zeroed bytes. node3 booted with 31 phantoms SIX SECONDS BEFORE the term activated; 24 of 31 resolve to txids -> 24 distinct shards all in the 31-shard failed set; the other 7 are exactly the 7 the harness reported overlapping; the nodes' own counters agree (n1 phantom_masters:7, n3:24, 24+7=31). The CRC constant 0x3F84EB45 being INVARIANT across every failure is what cracked it — it is the CRC over an all-zero METADATA_SIZE header. Pre-fix state is worse than a wedge: recovery FAILS OUTRIGHT (`Index(FormatError)`) — a boot-blocking poison pill.
  MY MECHANISM WAS HALF RIGHT: `reconcile_freelist_against_live_index` does not carve out in the common case — the offset was never on the freelist at all, so the phantom is simply NEVER CHALLENGED. Carve-out is one path of two.
  FIX (46c102e/b27d05b/246f9b3): commit-before-destroy. Rejected journaling the tombstone (a tombstone journal carries no information the FreeRegion doesn't — `evict_freed_region_owner` already converts a replayed FreeRegion into "this key is deleted"; the commit record wasn't missing, it was written in the WRONG ORDER). The naive reorder is unsafe (freeing first makes the region allocatable, so a concurrent create takes the offset and has its fresh header zeroed), so the free was SPLIT: `journal_free` (validate+append+fsync, no memory mutation) / `apply_journaled_free` (fold into freelist). Between them the region is committed-as-freed on disk yet un-allocatable in memory. ZERO added fsync — the same flush, earlier. Both engines + forwarder.
  ALSO FOUND, unasked: post-commit `?`s short-circuited PAST the tombstone record — a durable delete with NO tombstone, the reverse-heal resurrection vector — now deferred; and the SAME false guarantee lived in tombstone.rs, in the canonical place TS-1 is defined.
  REVIEWS (2, both 0 P0 / 0 P1) CREDITED TWO UNDERSOLD WINS: the pre-existing bug fixed is worse than "invisible until restart" (the next checkpoint snapshots an index without the key — PERMANENTLY losing a live acked record), and the reorder also FIXES BACKUP/RESTORE (any teed-tail cut capturing the destruction now necessarily contains the commit record). And caught a BOOT-BLOCKING P2 on the engine CI actually runs: `SegmentAllocator::apply_journaled_free` lacked the `used == 0` guard `replay_free` already had — the fixer then found `free` NEEDED IT TOO (spend relocate re-points the index before dead-marking, identical window).
- **THE RUSTDOC GATE KEEPS EARNING ITS KEEP** — it caught a link to a private enum in this wave's own doc rewrite (exit 101). Plain `cargo doc` would have warned and moved on.
- **TWO PROCESS FAILURES, BOTH INSTRUCTIVE.**
  (a) An agent ran `git checkout -- src/cluster/coordinator.rs` to undo a mutation-test edit and DESTROYED ~600 lines of its own uncommitted review fixes. `git fsck --unreachable` found nothing — content never staged has no blob. Rebuilt from notes; a later audit recomputed the delta and found it BYTE-IDENTICAL, so nothing was lost. Root cause was a REAL GAP IN THE RULES: stash is banned in worktrees, checkout/restore/reset is banned globally, and together they left no stated way to undo a deliberate temporary edit. CLAUDE.md now names the answer (copy to $CLAUDE_JOB_DIR/tmp, or commit first) — 0d712fc.
  (b) A round-2 hoist SILENTLY DEFANGED one of its own new invariant tests: moving a variable's definition 1002 chars outside a 400-char search window made the assertion match a local variable NAME instead of the authority call it existed to pin. The reviewer proved it by substituting the exact defect the test names in its own failure message — it PASSES SILENTLY. **A source-scanning invariant test that drifts out of window does not go red; it quietly stops checking.**
- DOC CONTRADICTION FOUND: fixing tombstone.rs left `docs/DURABILITY_CONTRACT.md` asserting the OPPOSITE — and the operator-facing doc was the stale one, which is worse than one wrong doc because a reader can now find confirmation for either belief. Fixed.
- Residuals tracked: #96 (absent-key delete records no tombstone), #97 (per-peer identity), #98 (PREVENTION — nobody has explained why an RF=2 WriteAll write left a hole at all; repair without prevention leaves a zero-redundancy window), Layer B (receiver-side replace-on-unreadable, so an ALREADY-poisoned node can heal — Layer A is preventive only).
